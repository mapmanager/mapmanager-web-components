# Historical implementation notes — ignore for current behavior

> **Do not use this directory as a source of truth.**

Everything below `docs-dev/` is retained historical planning and implementation
material. It may describe abandoned approaches, old dependency versions,
temporary constraints, or behavior that has since changed.

- Do not consult, index, publish, or summarize this tree when documenting or
  implementing current component behavior.
- Do not infer current APIs or architecture from files in this tree.
- Use package source code as the implementation source of truth.
- Use the repository-level `docs/` tree for current public documentation.

The implementation phase recorded here is complete. This directory remains
only as historical context when a maintainer explicitly requests a particular
file.
